install locally 
    python -m pip install -e .

update on pypi (make sure to change the version in setup.py) 
    python setup.py sdist
    python setup.py bdist_wheel --universal
    twine upload dist/*